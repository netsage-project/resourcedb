// GlobalNOC 2017

// Gets reverse DNS information for the CIDR address.
function getReverseDNS(cidr, onSuccess) {
    console.log ("in getReverseDNS");
/*
    onSuccess("8.8.8.8 google.comx");

    var args = cidr.split("/");
    var addr = args[0];

    var url = 'http://api.hackertarget.com/reversedns';
    url += '?q=' + addr;
    fetch(url, {
        method: 'get'
    }).then(function(response) {

            console.log(response);
            onSuccess(response);

    }).catch(function(err) {
        console.log(err);
    });
*/
}
